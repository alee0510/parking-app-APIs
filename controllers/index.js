// import cotroller
const users = require('./users')
const account = require('./account')
const profile = require('./profile')
const admin = require('./admin')
const history = require('./history')

// export controllers
module.exports = { users, account, profile, admin, history }